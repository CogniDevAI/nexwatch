services:
  hub:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.hub
    ports:
      - "8090:8090"
    environment:
      # Ensures the sample agent's "changeme" token below authenticates
      # against a hashed agents.token_hash record on first boot.
      - NEXWATCH_BOOTSTRAP_AGENT_TOKEN=changeme
    volumes:
      - nexwatch-data:/app/pb_data
    restart: unless-stopped

  agent:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.agent
    command:
      - "--hub=ws://hub:8090/ws/agent"
      - "--token=changeme"
      - "--interval=10"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - hub
    restart: unless-stopped

volumes:
  nexwatch-data:
