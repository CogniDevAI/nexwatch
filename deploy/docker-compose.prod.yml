services:
  hub:
    image: ghcr.io/cognidevai/nexwatch-hub:latest
    restart: unless-stopped
    environment:
      - TZ=${TZ:-America/Guayaquil}
    ports:
      - "${HUB_PORT:-8090}:8090"
    volumes:
      - nexwatch-data:/app/pb_data

volumes:
  nexwatch-data:
